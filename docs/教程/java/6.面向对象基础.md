# 第六章 面向对象基础

## 6.1 面向对象概述

面向对象编程（Object-Oriented Programming，OOP）是一种程序设计范式，它将数据和操作数据的方法组织在一起，形成对象。面向对象编程的核心概念包括类、对象、封装、继承和多态。

Java是一种纯粹的面向对象编程语言，所有代码都必须定义在类中。理解面向对象的概念是掌握Java编程的关键。

## 6.2 类与对象

### 类的定义

类是创建对象的模板，定义了对象的属性和行为：

```java
public class Student {
    // 成员变量（属性）
    private String name;
    private int age;
    private double score;
    
    // 构造方法
    public Student(String name, int age, double score) {
        this.name = name;
        this.age = age;
        this.score = score;
    }
    
    // 成员方法（行为）
    public void study() {
        System.out.println(name + "正在学习");
    }
    
    public void displayInfo() {
        System.out.println("姓名：" + name);
        System.out.println("年龄：" + age);
        System.out.println("成绩：" + score);
    }
    
    // getter和setter方法
    public String getName() {
        return name;
    }
    
    public void setName(String name) {
        this.name = name;
    }
    
    public int getAge() {
        return age;
    }
    
    public void setAge(int age) {
        if (age > 0) {
            this.age = age;
        }
    }
    
    public double getScore() {
        return score;
    }
    
    public void setScore(double score) {
        this.score = score;
    }
}
```

### 创建对象

```java
// 使用new关键字创建对象
Student student1 = new Student("Alice", 20, 85.5);
Student student2 = new Student("Bob", 21, 90.0);

// 调用方法
student1.displayInfo();
student1.study();

// 使用getter和setter
System.out.println(student1.getName());
student1.setScore(88.0);
```

## 6.3 成员变量

成员变量定义在类中，描述对象的属性：

### 实例变量

每个对象独立拥有的变量：

```java
public class Person {
    private String name;        // 实例变量
    private int age;            // 实例变量
}
```

### 类变量（静态变量）

所有对象共享的变量，使用static修饰：

```java
public class Counter {
    private static int count = 0;   // 类变量
    
    public Counter() {
        count++;                    // 每创建一个对象，计数器加1
    }
    
    public static int getCount() {
        return count;
    }
}
```

### 变量初始化

```java
public class Example {
    // 直接初始化
    private int a = 10;
    
    // 构造方法中初始化
    private int b;
    public Example() {
        b = 20;
    }
    
    // 初始化块
    private int c;
    {
        c = 30;
    }
    
    // 静态初始化块
    private static int d;
    static {
        d = 40;
    }
}
```

## 6.4 方法

### 方法的定义

```java
修饰符 返回类型 方法名(参数列表) {
    // 方法体
    return 返回值;  // 如果返回类型不是void
}
```

```java
public int add(int a, int b) {
    return a + b;
}

public void printMessage(String message) {
    System.out.println(message);
}
```

### 方法重载

同一个类中可以定义多个同名方法，只要参数列表不同：

```java
public class Calculator {
    
    public int add(int a, int b) {
        return a + b;
    }
    
    public double add(double a, double b) {
        return a + b;
    }
    
    public int add(int a, int b, int c) {
        return a + b + c;
    }
}
```

### 可变参数方法

```java
public int sum(int... numbers) {
    int total = 0;
    for (int num : numbers) {
        total += num;
    }
    return total;
}
```

### 静态方法

使用static修饰的方法，属于类而非对象：

```java
public class MathUtils {
    
    public static int max(int a, int b) {
        return (a > b) ? a : b;
    }
    
    public static double circleArea(double radius) {
        return Math.PI * radius * radius;
    }
}

// 调用
int max = MathUtils.max(3, 5);
double area = MathUtils.circleArea(5.0);
```

## 6.5 构造方法

构造方法用于创建和初始化对象：

### 默认构造方法

如果没有定义构造方法，编译器会提供默认的无参构造方法：

```java
public class Person {
    private String name;
    private int age;
    
    // 编译器自动提供：
    // public Person() {}
}
```

### 自定义构造方法

```java
public class Person {
    private String name;
    private int age;
    
    // 无参构造方法
    public Person() {
        this.name = "Unknown";
        this.age = 0;
    }
    
    // 带参数的构造方法
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // 构造方法重载
    public Person(String name) {
        this(name, 0);      // 调用另一个构造方法
    }
}
```

### 构造方法调用

```java
Person p1 = new Person();                   // 调用无参构造
Person p2 = new Person("Alice", 20);        // 调用带参数构造
Person p3 = new Person("Bob");              // 调用单参数构造
```

## 6.6 this关键字

this关键字引用当前对象：

### 区分成员变量和参数

```java
public Person(String name, int age) {
    this.name = name;       // this.name指成员变量，name指参数
    this.age = age;
}
```

### 调用其他构造方法

```java
public Person(String name) {
    this(name, 0);          // 调用另一个构造方法
}
```

### 返回当前对象

```java
public class Builder {
    private String name;
    private int age;
    
    public Builder setName(String name) {
        this.name = name;
        return this;        // 支持链式调用
    }
    
    public Builder setAge(int age) {
        this.age = age;
        return this;
    }
}

// 链式调用
Builder builder = new Builder()
    .setName("Alice")
    .setAge(20);
```

## 6.7 封装

封装是将数据和对数据的操作包装在一起，隐藏内部实现细节，只暴露必要的接口：

```java
public class BankAccount {
    private String accountNumber;
    private double balance;
    
    public BankAccount(String accountNumber, double initialBalance) {
        this.accountNumber = accountNumber;
        this.balance = initialBalance;
    }
    
    // 存款
    public void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
        }
    }
    
    // 取款
    public boolean withdraw(double amount) {
        if (amount > 0 && amount <= balance) {
            balance -= amount;
            return true;
        }
        return false;
    }
    
    // 查询余额
    public double getBalance() {
        return balance;
    }
    
    // 账号不允许修改，只提供getter
    public String getAccountNumber() {
        return accountNumber;
    }
}
```

封装的优点：
- 隐藏实现细节，提高安全性
- 便于修改内部实现而不影响外部代码
- 可以对数据进行验证和控制

## 6.8 包

包用于组织类和接口，避免命名冲突：

### 包的声明

```java
package com.example.utils;

public class StringUtils {
    // ...
}
```

### 包的导入

```java
// 导入单个类
import java.util.Scanner;

// 导入包中所有类
import java.util.*;

// 静态导入
import static java.lang.Math.*;

public class Example {
    public static void main(String[] args) {
        double result = sqrt(16);   // 直接使用，无需Math.前缀
    }
}
```

### 常用包

| 包名 | 说明 |
|------|------|
| java.lang | 核心类，自动导入 |
| java.util | 工具类，如集合、日期等 |
| java.io | 输入输出类 |
| java.net | 网络编程类 |
| java.math | 数学运算类 |

## 6.9 访问修饰符

访问修饰符控制类、方法和变量的可见性：

| 修饰符 | 同类 | 同包 | 子类 | 所有类 |
|--------|------|------|------|--------|
| public | 是 | 是 | 是 | 是 |
| protected | 是 | 是 | 是 | 否 |
| 默认（无） | 是 | 是 | 否 | 否 |
| private | 是 | 否 | 否 | 否 |

```java
public class Example {
    public int publicVar;           // 所有类可访问
    protected int protectedVar;     // 同包或子类可访问
    int defaultVar;                 // 同包可访问
    private int privateVar;         // 仅同类可访问
    
    public void publicMethod() {}
    protected void protectedMethod() {}
    void defaultMethod() {}
    private void privateMethod() {}
}
```

## 6.10 综合示例

### 示例1：矩形类

```java
public class Rectangle {
    private double width;
    private double height;
    
    public Rectangle() {
        this(1.0, 1.0);
    }
    
    public Rectangle(double width, double height) {
        this.width = width;
        this.height = height;
    }
    
    public double getWidth() {
        return width;
    }
    
    public void setWidth(double width) {
        if (width > 0) {
            this.width = width;
        }
    }
    
    public double getHeight() {
        return height;
    }
    
    public void setHeight(double height) {
        if (height > 0) {
            this.height = height;
        }
    }
    
    public double getArea() {
        return width * height;
    }
    
    public double getPerimeter() {
        return 2 * (width + height);
    }
    
    @Override
    public String toString() {
        return "Rectangle[width=" + width + ", height=" + height + "]";
    }
    
    public static void main(String[] args) {
        Rectangle rect1 = new Rectangle();
        Rectangle rect2 = new Rectangle(5.0, 3.0);
        
        System.out.println(rect2);
        System.out.println("面积：" + rect2.getArea());
        System.out.println("周长：" + rect2.getPerimeter());
    }
}
```

### 示例2：日期类

```java
public class MyDate {
    private int year;
    private int month;
    private int day;
    
    public MyDate(int year, int month, int day) {
        this.year = year;
        this.month = month;
        this.day = day;
    }
    
    public MyDate() {
        this(2024, 1, 1);
    }
    
    public int getYear() {
        return year;
    }
    
    public void setYear(int year) {
        this.year = year;
    }
    
    public int getMonth() {
        return month;
    }
    
    public void setMonth(int month) {
        if (month >= 1 && month <= 12) {
            this.month = month;
        }
    }
    
    public int getDay() {
        return day;
    }
    
    public void setDay(int day) {
        if (day >= 1 && day <= 31) {
            this.day = day;
        }
    }
    
    public String toString() {
        return String.format("%04d-%02d-%02d", year, month, day);
    }
    
    public boolean isLeapYear() {
        return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
    }
    
    public static void main(String[] args) {
        MyDate date1 = new MyDate(2024, 2, 15);
        MyDate date2 = new MyDate();
        
        System.out.println("日期1：" + date1);
        System.out.println("是否闰年：" + date1.isLeapYear());
        System.out.println("日期2：" + date2);
    }
}
```

## 6.11 本章小结

本章详细介绍了Java面向对象编程的基础知识，包括类与对象的概念、成员变量和方法的定义、构造方法的使用、this关键字的应用、封装的原则、包的组织方式和访问修饰符的作用。掌握这些知识是理解面向对象编程和构建复杂Java应用程序的基础。
